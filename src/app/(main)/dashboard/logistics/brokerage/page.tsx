/** RUNTIME CLASSIFICATION: MOCK — brokerage-data.ts; not in production navigation. */
import { BrokerageDocs } from "./_components/brokerage";

export default function Page() {
  return <BrokerageDocs />;
}
