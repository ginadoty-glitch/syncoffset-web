/** RUNTIME CLASSIFICATION: MOCK — notifications-data.ts; not in production navigation. */
import { Notifications } from "../_components/notifications";

export default function Page() {
  return <Notifications />;
}
