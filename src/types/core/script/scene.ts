/**
 * @deprecated Scene Authority owns the Scene type — import from `src/types/core/scene`.
 * Re-export preserved for Script Authority barrel compatibility.
 */
export type { Scene } from "../scene";
